#ifndef HEADFILE_H
#define HEADFILE_H

#include <stdarg.h>
#include <unistd.h>
#include <string.h>

/** my prototypes */
int _printf(const char *format, ...);
int _putchar(char c);
int get(int n);
#endif
