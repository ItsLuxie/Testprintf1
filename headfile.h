#ifndef HEADFILE_H
#define HEADFILE_H

#include <stdarg.h>
#include <unistd.h>

/** my prototypes */
int _putchar(char c);
int _print(char *string);
int _printf(const char *format, ...);
int get(int n);
#endif
